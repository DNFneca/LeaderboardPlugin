package me.DNFneca.leaderboard.utils.config;

public class Config {
    public static final String configFileName = "config.yml";


    // TODO: make a config class for reading and setting all of the properties in the config.yml file and making use of them, first thing to do is finish the automatic migration to the new boards, need to make regenerating leaderboards if they get destroyed and maybe mess with nms to display separate things to other players
}
